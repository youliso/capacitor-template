import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capacitor.test',
  appName: 'capacitor-web',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
